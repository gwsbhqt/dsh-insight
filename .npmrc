# 安装走 npmmirror（npm 官方源的公开镜像）。
#
# 为什么要在仓库里钉死：开发机的 npm 默认源可能是某个内网 registry。那样一来
# 装依赖会经过内网源，重新生成的 lockfile 也可能带上内网痕迹——而这是个公开仓库。
# 钉在这里之后，机器上的全局配置再也影响不到它。
#
# 发布不走这里：npmmirror 是只读镜像。发布目标由 package.json 的
# publishConfig.registry 指定为 registry.npmjs.org，publishConfig 的优先级高于本文件。
#
# 这个文件里永远不要写 _authToken。
registry=https://registry.npmmirror.com
